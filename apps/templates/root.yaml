apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: root-applicationset
spec:
  generators:
  - list:
      elements:
      - name: gitops-test
        repoURL: https://github.com/mtvier/gitops_test.git
        path: apps/
        namespace: default
      - name: mysql-cluster
        repoURL: https://github.com/mtvier/my_k8s_service.git
        path: apps/
        namespace: my_k8s_service
  template:
    metadata:
      name: '{{name}}'
    spec:
      project: default
      source:
        repoURL: '{{repoURL}}'
        targetRevision: HEAD
        path: '{{path}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{namespace}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
